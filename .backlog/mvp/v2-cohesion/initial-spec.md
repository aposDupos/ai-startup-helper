# V2: Целостность, вовлечение и ежедневный возврат

## Проблема

После 5 спринтов платформа представляет собой набор инструментов без единого ритма. Пользователь не чувствует целостности процесса и не имеет мотивации возвращаться.

---

## Аудит Sprint 05: что реализовано, что НЕ подключено

### ✅ Код НАПИСАН (файлы существуют):

| Компонент | Файлы | Статус |
|-----------|-------|--------|
| **Pitch Deck миграция** | `008_pitch_decks.sql` | ✅ Создана |
| **Gamification миграция** | `009_gamification.sql` (achievements, xp_transactions, levels, challenges + 18 seed achievements + 5 levels) | ✅ Создана |
| **XP система** | `src/lib/gamification/xp.ts` — `awardXP()`, `getUserXPInfo()` | ✅ Написан |
| **Стрики** | `src/lib/gamification/streaks.ts` — `updateStreak()`, `getStreakInfo()` с milestones (3/7/14/30/60/100 дней) | ✅ Написан |
| **Достижения** | `src/lib/gamification/achievements.ts` — `checkAchievements()` с evaluateCriteria для 8 типов | ✅ Написан |
| **Лидерборд** | `src/lib/gamification/leaderboard.ts` | ✅ Написан |
| **UI: XPBar** | `src/components/gamification/XPBar.tsx` | ✅ Написан |
| **UI: AchievementsGrid** | `src/components/gamification/AchievementsGrid.tsx` | ✅ Написан |
| **UI: StreakFlame** | `src/components/gamification/StreakFlame.tsx` | ✅ Написан |
| **UI: LeaderboardTable** | `src/components/gamification/LeaderboardTable.tsx` | ✅ Написан |
| **Страница Gamification** | `src/app/(main)/gamification/page.tsx` — отображает XP, стрик, бейджи, лидерборд | ✅ Написан |
| **Pitch Wizard** | `src/app/(main)/workspace/pitch/page.tsx` + `PitchDeckWizard.tsx` | ✅ Написан |
| **Pitch Trainer** | `src/app/(main)/workspace/pitch/trainer/page.tsx` + `PitchTrainer.tsx` | ✅ Написан |
| **Pitch PDF** | `src/lib/export/pitch-pdf.tsx` | ✅ Написан |

### ❌ КРИТИЧЕСКИЕ ПРОБЕЛЫ (код не подключён):

| Проблема | Детали |
|----------|--------|
| **`updateStreak()` НИКОГДА НЕ ВЫЗЫВАЕТСЯ** | Функция определена в `streaks.ts` но не вызывается ни в middleware, ни в layout, ни при заходе пользователя. Стрик ВСЕГДА = 0 |
| **`awardXP()` НЕ ВЫЗЫВАЕТСЯ ИЗ ПОЛЬЗОВАТЕЛЬСКИХ ДЕЙСТВИЙ** | Вызывается только из `achievements.ts` и `streaks.ts` (внутренние вызовы). Нет XP за: урок, квиз, чеклист, артефакт, чат |
| **`checkAchievements()` НЕ ТРИГГЕРИТСЯ** | Вызывается только из `getUserAchievements()` при открытии страницы `/gamification`. Не триггерится при реальных действиях |
| **Sidebar → `/academy` = 404** | Sidebar имеет пункт `{ href: "/academy", label: "Academy" }`, но такой маршрут НЕ СУЩЕСТВУЕТ. Нет ни `/academy/page.tsx` ни `/learning/page.tsx` |
| **Chat → `?context=` параметр ИГНОРИРУЕТСЯ** | `CreateProjectWidget` отправляет на `/chat?context=idea_search`, но `chat/page.tsx` НЕ читает query params. Контекст всегда `"general"` |
| **Sidebar показывает hardcoded данные** | `Sidebar.tsx` line 82: `⭐ Lvl 1` — захардкожено, не читает реальный уровень и XP |
| **Dashboard XP/Streak — отображает но НЕ обновляет** | Dashboard показывает `profile?.xp` и `profile?.streak_count`, но `updateStreak()` нигде не вызывается → streak всегда 0 |

---

## Проблемы UX, найденные при тестировании

### 1. ContextSwitcher — путаница с «специалистами»

**Текущее:** `ContextSwitcher.tsx` показывает 5 вкладок (💡Идея, 🔍Валидация, 📊БМ, 🚀MVP, 🎯Питч) которые пользователь должен выбирать вручную.

**Проблема:** Пользователь не понимает зачем это и какого «специалиста» выбрать.

**Решение:** Убрать ContextSwitcher как кликабельный элемент. AI автоматически определяет стадию из:
- `project.stage` (текущая стадия проекта)  
- `?context=` URL-параметра (от entry points)
- Текста сообщения (AI fallback)

В UI показать как read-only badge: "🔍 Валидация — текущая стадия".

### 2. Страница Academy отсутствует

**Текущее:**
- Sidebar → `/academy` → 404
- `/learning/` содержит только `actions.ts`, нет `page.tsx`  
- `/gamification/page.tsx` существует но отдельно
- Inline-уроки работают через чеклисты

**Решение:** Создать `/learning/page.tsx` — обзорная страница:
- Список всех уроков, сгруппированных по стадиям
- Прогресс ✅ / ❌ по каждому
- Рекомендация «что пройти дальше» на основе стадии проекта
- Обновить Sidebar: `/academy` → `/learning`

### 3. Прогресс не обновляется при заполнении артефактов через чат

**Текущее:**
- `update-project-artifacts.ts` содержит `ARTIFACT_TO_CHECKLIST` маппинг и вызывает `executeCompleteChecklist()`
- Ошибки auto-complete поглощаются через `console.warn` (line 89)
- Возможно, ключи маппинга не совпадают с реальными `stage_checklists` записями в БД
- Dashboard не получает `revalidatePath` после API-обновлений

**Решение:**
1. Проверить совпадение ключей `ARTIFACT_TO_CHECKLIST` с seed data в `stage_checklists`
2. Убрать тихое поглощение ошибок — логировать + возвращать пользователю
3. Добавить `revalidatePath('/dashboard')` в API endpoint после обновления

---

## Пользовательские пути (User Journeys)

### Текущее состояние

`CreateProjectWidget` определяет 4 entry points:

| # | Кнопка | href | Реальный эффект |
|---|--------|------|-----------------|
| 1 | 💡 "У меня нет идеи" | `/chat?context=idea_search` | ❌ Открывает чат в контексте `"general"` (param игнорируется) |
| 2 | 🎯 "У меня есть идея" | `/chat?context=idea_evaluation` | ❌ Тоже `"general"` |
| 3 | 🚀 "У меня уже проект" | `/chat?context=project_assessment` | ❌ Тоже `"general"` |
| 4 | 📚 "Хочу поучиться" | `/chat?context=learning` | ❌ Тоже `"general"` + нет страницы обучения |

**Все 4 пути ведут в одно и то же место и работают одинаково!**

### Желаемое поведение

| # | Путь | Что должно происходить |
|---|------|----------------------|
| 1 | **Нет идеи** | Чат в контексте `idea_search` → AI ведёт по шагам: Проблема → ЦА → Идея → ICE оценка. При каждом артефакте → ✅ чеклист + XP |
| 2 | **Есть идея** | AI просит описать идею → автоматически создаёт проект + определяет стадию → показывает Journey Map → ведёт по чеклисту |
| 3 | **Есть проект** | AI задаёт вопросы → определяет стадию (validation/bmc/mvp/pitch) → создаёт проект на нужной стадии → разблокирует предыдущие стадии как completed |
| 4 | **Хочу учиться** | Открывает страницу `/learning` → рекомендует уроки → после урока предлагает вернуться к созданию проекта |

---

## Адаптация уроков под проект

### Текущее
Уроки статичные: 10 seed-уроков с JSONB контентом. Привязаны к стадиям (idea, validation, bmc, mvp, pitch). Контент одинаков для всех пользователей.

### Предложение

1. **Контекстные примеры в уроках:** При рендеринге урока подставлять данные проекта:
   - Урок "Что такое CustDev" → "Для проекта «{project.title}» вопрос CustDev может звучать так: ..."
   - Урок "ICE-оценка" → "Давай оценим твою идею «{project.idea}» по ICE: ..."

2. **AI-рекомендация урока в чате:** Когда пользователь затрудняется, AI предлагает конкретный урок и объясняет как он поможет именно с текущей задачей

3. **Quiz = про свой проект:** Вместо абстрактных вопросов ("Что такое CustDev?") — вопросы в контексте проекта ("Какой главный риск в проекте «{title}»?")

---

## Retention-механики (Hook Loop)

### Daily Quest
- Одна микро-задача каждый день (3-5 мин) на Dashboard
- AI генерирует квест на основе стадии + progress_data
- При выполнении → +XP toast → обновление стрика

### Стрики 🔥
- `updateStreak()` вызывается при каждом заходе (в layout или middleware)
- Огонёк 🔥 + счётчик в Sidebar (вместо хардкода "⭐ Lvl 1")
- Бонус XP на вехах

### XP Loop
- Начисление через `awardXP()` при: уроке, квизе, чеклисте, артефакте, стрике
- Toast "+20 XP" с анимацией
- Level up модалка
- Sidebar показывает реальный level + XP

### Milestone Celebrations
- Confetti при завершении стадии
- Badge unlock модалка  
- AI-поздравление

---

## Архитектурные ограничения

- Stack: Next.js 15 + Tailwind v4 + Supabase + GigaChat (langchain-gigachat)
- Миграции: 001-011 (включая gamification 009)
- UI: shadcn/ui + framer-motion
- Дизайн: Plus Jakarta Sans + Inter, тёплые stone-нейтральные

## Приоритет

1. **Critical (integration):** Подключить gamification к user flow (streak, XP, achievements)
2. **Critical (bug):** Починить `?context=` в chat — entry points реально работают
3. **Critical (bug):** Починить progress tracking (ARTIFACT_TO_CHECKLIST маппинг)
4. **High (UX):** Убрать ContextSwitcher → auto-routing AI
5. **High (UX):** Создать страницу `/learning` + починить Sidebar навигацию
6. **High (UX):** Sidebar: реальные данные пользователя (level, XP, streak)
7. **High (retention):** Daily Quest на Dashboard
8. **Medium (UX):** Адаптация уроков под проект пользователя
9. **Medium (retention):** Celebration модалки (confetti, badge unlock, level up)
10. **Low (retention):** Weekly Report, Sharing
